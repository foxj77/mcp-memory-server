{
  "name": "mcp-memory-server",
  "private": true,
  "description": "Tracks Dockerfile npm dependencies for Dependabot version updates. Not installed locally — versions here must stay in sync with the RUN npm install -g line in the Dockerfile.",
  "dependencies": {
    "@modelcontextprotocol/server-memory": "2026.1.26",
    "supergateway": "3.4.3"
  }
}
